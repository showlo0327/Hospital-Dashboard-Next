<script setup lang="ts">
/**
 * Architecture placeholder for future virtual-list integration.
 *
 * When implementing virtual scrolling:
 *   1. Replace DeviceListContent with a virtual list (e.g. @tanstack/vue-virtual)
 *   2. Pass `estimateSize` matching DeviceListItem height (~58px)
 *   3. Use this component as the overscan / loading row fallback
 *
 * Current: renders nothing. Exists solely as an architectural anchor.
 *
 * Component Maturity: Experimental
 * Placeholder only — no rendering, no logic.
 */
</script>

<template>
  <!-- Future: virtual list integration point -->
  <div />
</template>
