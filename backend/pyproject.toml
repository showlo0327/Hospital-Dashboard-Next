[project]
name = "hospital-dashboard-api"
version = "2.8.0"
description = "Hospital Network Operations Platform — Backend API"
requires-python = ">=3.11"

[tool.uvicorn]
host = "0.0.0.0"
port = 8000
reload = true
